apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - immich-helm.yaml
configMapGenerator:
  - name: immich-values
    namespace: immich
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
