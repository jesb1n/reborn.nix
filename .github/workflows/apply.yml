name: Terraform Apply

on:
  workflow_dispatch: # Manual trigger from GitHub Actions UI

jobs:
  apply:
    name: Plan & Apply
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: 1.9.1

      - name: Configure S3 backend credentials
        run: |
          mkdir -p ~/.aws
          cat > ~/.aws/credentials <<EOF
          [default]
          aws_access_key_id=${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key=${{ secrets.AWS_SECRET_ACCESS_KEY }}
          EOF

      - name: Terraform Init
        run: tofu -chdir=IaC init

      - name: Terraform Validate
        run: tofu -chdir=IaC validate

      - name: Terraform Plan
        run: tofu -chdir=IaC plan -out=tfplan

      - name: Terraform Apply
        run: tofu -chdir=IaC apply -auto-approve tfplan

      - name: Recover state on failure
        if: failure()
        run: |
          if [ -f IaC/errored.tfstate ]; then
            echo "Pushing IaC/errored.tfstate to remote backend..."
            tofu -chdir=IaC state push IaC/errored.tfstate || true
          fi
